apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: state-message
spec:
  type: state.postgresql
  version: v1
  metadata:
  - name: connectionString
    value: "host=localhost user=wuser password=wpass port=54321 connect_timeout=10 database=scheduled-messages"
